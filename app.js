const tokens = JSON.parse(localStorage.getItem('inteagent-tokens') || '[]');
/*
  {id:'opencompute',name:'OpenCompute',ticker:'$COMPUTE',initials:'OC',description:'Funding open-source AI infrastructure.',mission:'Use generated fees to purchase cloud compute and fund open-source AI infrastructure.',fees:'$24,820',balance:'$8,420',actions:37,marketCap:'$2.8M',volume:'$184K',holders:'4,218',activity:['Purchased compute credits','Allocated infrastructure grant','Paid API infrastructure']},
  {id:'publicgoods',name:'PublicGoods',ticker:'$GOODS',initials:'PG',description:'A treasury for digital public goods.',mission:'Route fees to maintain public software, open protocols and the people who build them.',fees:'$18,402',balance:'$5,910',actions:24,marketCap:'$1.9M',volume:'$92K',holders:'2,806',activity:['Transferred open-source grant','Funded protocol audit','Published treasury report']},
  {id:'creatorfund',name:'CreatorFund',ticker:'$CREATOR',initials:'CF',description:'Backing independent creators with every trade.',mission:'Fund small creative projects and distribute transparent micro-grants to independent creators.',fees:'$12,631',balance:'$3,275',actions:19,marketCap:'$1.4M',volume:'$71K',holders:'1,966',activity:['Issued creator grant','Paid production tools','Added grant recipient']},
  {id:'datadao',name:'DataDAO',ticker:'$DATA',initials:'DD',description:'Community-owned data research, done openly.',mission:'Acquire high-quality datasets and fund research that keeps data access transparent.',fees:'$31,092',balance:'$14,008',actions:52,marketCap:'$3.7M',volume:'$240K',holders:'5,402',activity:['Acquired dataset license','Funded research sprint','Published allocation']},
  {id:'greengrid',name:'GreenGrid',ticker:'$GRID',initials:'GG',description:'Routing capital toward cleaner energy systems.',mission:'Support verifiable clean-energy initiatives and publish the impact of every allocation.',fees:'$9,810',balance:'$2,988',actions:16,marketCap:'$980K',volume:'$45K',holders:'1,203',activity:['Purchased impact credits','Funded solar assessment','Verified allocation']},
  {id:'research',name:'Research',ticker:'$RESEARCH',initials:'R',description:'Patient funding for difficult questions.',mission:'Fund independent scientific research selected through an open and auditable process.',fees:'$27,440',balance:'$11,204',actions:43,marketCap:'$3.1M',volume:'$156K',holders:'3,810',activity:['Funded research proposal','Paid compute credits','Opened next round']},
  {id:'openmodels',name:'OpenModels',ticker:'$MODELS',initials:'OM',description:'Tools and models that stay open.',mission:'Buy compute and support contributors building openly licensed machine learning models.',fees:'$42,700',balance:'$17,400',actions:68,marketCap:'$4.2M',volume:'$318K',holders:'7,124',activity:['Bought training compute','Paid contributor bounty','Released model credits']},
  {id:'devfund',name:'DevFund',ticker:'$DEV',initials:'DF',description:'A programmable fund for builders.',mission:'Pay for developer tools, security reviews and the maintenance work communities depend on.',fees:'$16,215',balance:'$7,300',actions:29,marketCap:'$1.8M',volume:'$88K',holders:'2,430',activity:['Paid security review','Funded maintainer','Bought service credits']}
]; */
const app = document.querySelector('#app');
const promptDefault = '';
let launchState = {name:'', ticker:'', description:'', prompt:promptDefault};
let filter = 'All'; let query = '';
const esc = value => String(value).replace(/[&<>"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const icon = name => `<i data-lucide="${name}"></i>`;
function shell(content){app.innerHTML = content; lucide.createIcons(); window.scrollTo(0,0);queueMicrotask(()=>{setupImageUpload();setupNetworkNote();bindWalletButton()})}
function home(){shell(`<div class="container"><section class="hero"><div class="hero-copy"><p class="eyebrow">Autonomous token infrastructure</p><h1>Launch tokens<br><em>with a mission.</em></h1><p class="lede">Every token gets an AI agent. Trading fees fund the mission you define.</p><div class="actions"><a class="button button-primary" href="#/launch" data-route>Launch a token ${icon('arrow-up-right')}</a><a class="button button-secondary" href="#/discover">Discover agents ${icon('move-right')}</a></div></div><div class="signal"><div class="signal-core">AI</div></div></section></div><section class="flow-section"><div class="container"><div class="flow"><div class="flow-step"><span>01</span><strong>Token</strong><i>→</i></div><div class="flow-step"><span>02</span><strong>Trading fees</strong><i>→</i></div><div class="flow-step"><span>03</span><strong>AI agent</strong><i>→</i></div><div class="flow-step"><span>04</span><strong>Mission</strong></div></div></div></section><section class="section"><div class="container"><div class="section-header"><h2>How it works</h2><p class="section-sub">Write the rules. The agent executes the mission.</p></div><div class="steps"><div class="step"><span class="step-number">01</span><h3>Launch</h3><p>Create a token and define its AI agent.</p></div><div class="step"><span class="step-number">02</span><h3>Set the mission</h3><p>Write a prompt describing what fees should fund.</p></div><div class="step"><span class="step-number">03</span><h3>Trade</h3><p>Users trade the token and generate fees.</p></div><div class="step"><span class="step-number">04</span><h3>Agent acts</h3><p>Fees move according to the mission you wrote.</p></div></div></div></section><section class="section" style="padding-top:0"><div class="container"><div class="section-header"><h2>One live example</h2><p class="section-sub">A transparent mission, from token to action.</p></div>${exampleCard()}</div></section><section class="final-cta"><div class="container"><p class="eyebrow">Make it executable</p><h2>Give your token a mission.</h2><a class="button button-primary" href="#/launch" data-route>Launch a token ${icon('arrow-up-right')}</a></div></section>`)}
function exampleCard(){if(!tokens.length)return `<div class="empty-state"><p class="eyebrow">Registry empty</p><h3>No tokens launched yet</h3><p>Your first token and its agent will appear here after launch.</p><a class="button button-primary" href="#/launch" data-route>Launch the first token ${icon('arrow-up-right')}</a></div>`;const t=tokens[0];return `<div class="example-card"><div><div class="example-meta"><div class="token-orb">${t.initials}</div><div><div class="token-name">${t.name}</div><div class="ticker">${t.ticker}</div></div></div><p class="mission">“${t.mission}”</p><div class="status-line"><span class="status-dot"></span>Agent status: Active</div></div><div class="metrics"><div><span class="metric-label">Fees generated</span><strong class="metric-value">${t.fees}</strong></div><div><span class="metric-label">Agent balance</span><strong class="metric-value">${t.balance}</strong></div><div><span class="metric-label">Actions executed</span><strong class="metric-value">${t.actions}</strong></div></div></div>`}
function launch(){shell(`<div class="container"><section class="page-head"><p class="eyebrow">Creator workspace</p><h1>Launch a token</h1><p>Create a token and define the agent behind it.</p></section><div class="launch-grid"><div><section class="form-section"><h2>Token information</h2><p>The public details people will see when they discover your token.</p><div class="field-row"><div class="field"><label for="token-name">Token name</label><input id="token-name" value="${esc(launchState.name)}" placeholder="OpenCompute"></div><div class="field"><label for="token-ticker">Ticker</label><input id="token-ticker" value="${esc(launchState.ticker)}" placeholder="$TOKEN"></div></div><div class="field"><label for="token-description">Description</label><input id="token-description" placeholder="What does this token make possible?"></div><div class="field-row"><div class="field"><label>Website</label><input placeholder="https://"></div><div class="field"><label>X / Twitter</label><input placeholder="@handle"></div></div><div class="upload">${icon('upload')} Add a token image <span>· optional</span></div></section><section class="form-section"><h2>Define your agent</h2><p>Tell the agent what should happen with the fees generated by this token.</p><div class="field"><label for="agent-prompt">Agent prompt</label><textarea id="agent-prompt" placeholder="Describe the mission, priorities and boundaries for your agent.">${esc(launchState.prompt)}</textarea></div><p class="field-hint">Your prompt defines the agent's mission.</p><div class="prompt-presets"><button class="preset" data-prompt="Fund open source">Fund open source</button><button class="preset" data-prompt="Buy compute">Buy compute</button><button class="preset" data-prompt="Support creators">Support creators</button><button class="preset" data-prompt="Donate to a cause">Donate to a cause</button><button class="preset" data-prompt="Custom mission">Custom mission</button></div></section></div><aside class="preview-sticky"><div class="preview-card"><div class="preview-head"><span>AGENT PREVIEW</span><span class="preview-live"><span class="status-dot"></span>Live</span></div><div class="preview-token"><strong id="preview-name">${esc(launchState.name)}</strong><span id="preview-ticker">${esc(launchState.ticker)}</span></div><span class="preview-label">Mission</span><p class="preview-prompt" id="preview-prompt">${esc(launchState.prompt)}</p><div class="preview-destination"><span>Fee destination</span><span>AI Agent</span></div></div><div class="summary"><div class="summary-row"><span>Token</span><span id="summary-name">${esc(launchState.name)}</span></div><div class="summary-row"><span>Ticker</span><span id="summary-ticker">${esc(launchState.ticker)}</span></div><div class="summary-row"><span>Agent mission</span><span id="summary-mission">Ready to define</span></div><div class="summary-row"><span>Fee destination</span><span>AI Agent</span></div></div><button class="button button-primary" data-action="launch-submit">Launch token ${icon('arrow-up-right')}</button></aside></div></div>`);bindLaunch()}
function bindLaunch(){const name=document.querySelector('#token-name'),ticker=document.querySelector('#token-ticker'),prompt=document.querySelector('#agent-prompt');const update=()=>{launchState={name:name.value||'ExampleToken',ticker:ticker.value||'$EXAMPLE',prompt:prompt.value||'Define a mission for this agent.'};document.querySelector('#preview-name').textContent=launchState.name;document.querySelector('#preview-ticker').textContent=launchState.ticker;document.querySelector('#preview-prompt').textContent=launchState.prompt;document.querySelector('#summary-name').textContent=launchState.name;document.querySelector('#summary-ticker').textContent=launchState.ticker;document.querySelector('#summary-mission').textContent=launchState.prompt.length>28?launchState.prompt.slice(0,28)+'…':launchState.prompt};[name,ticker,prompt].forEach(el=>el.addEventListener('input',update));document.querySelectorAll('[data-prompt]').forEach(btn=>btn.addEventListener('click',()=>{const preset=btn.dataset.prompt;const texts={'Fund open source':'Use generated fees to fund open-source software and public goods. Prioritize projects with transparent budgets and measurable impact.','Buy compute':'Use generated fees to purchase cloud compute for open models. Optimize for useful training runs and publish a transparent spend report.','Support creators':'Use generated fees to issue small, transparent grants to independent creators building useful public work.','Donate to a cause':'Use generated fees to donate to verified organizations working on climate resilience and community health.','Custom mission':''};prompt.value=texts[preset];prompt.focus();update()}))}
function bindLaunch(){const fields=document.querySelectorAll('.field input'),name=document.querySelector('#token-name'),ticker=document.querySelector('#token-ticker'),description=fields[2],prompt=document.querySelector('#agent-prompt');const update=()=>{launchState={name:name.value,ticker:ticker.value,description:description.value,prompt:prompt.value};document.querySelector('#preview-name').textContent=launchState.name||'Your token';document.querySelector('#preview-ticker').textContent=launchState.ticker||'$TICKER';document.querySelector('#preview-prompt').textContent=launchState.prompt||'Your agent mission will appear here.';document.querySelector('#summary-name').textContent=launchState.name||'Not defined';document.querySelector('#summary-ticker').textContent=launchState.ticker||'Not defined';document.querySelector('#summary-mission').textContent=launchState.prompt?launchState.prompt.slice(0,28)+(launchState.prompt.length>28?'…':''):'Not defined'};[name,ticker,description,prompt].forEach(el=>el.addEventListener('input',update));document.querySelectorAll('[data-prompt]').forEach(btn=>btn.addEventListener('click',()=>{const preset=btn.dataset.prompt;const texts={'Fund open source':'Use generated fees to fund open-source software and public goods.','Buy compute':'Use generated fees to purchase cloud compute for open models.','Support creators':'Use generated fees to issue transparent grants to independent creators.','Donate to a cause':'Use generated fees to donate to verified organizations.','Custom mission':''};prompt.value=texts[preset];prompt.focus();update()}))}
function discover(){const visible=tokens.filter(t=>(filter==='All'||filter==='Active'||filter==='New'||filter==='Trending')&&(!query||`${t.name} ${t.ticker} ${t.description} ${t.mission}`.toLowerCase().includes(query.toLowerCase())));shell(`<div class="container"><section class="page-head"><p class="eyebrow">The agent registry</p><h1>Discover</h1><p>Explore tokens and the agents behind them.</p></section><div class="discover-toolbar"><div class="search-wrap">${icon('search')}<input class="search-box" id="search" value="${esc(query)}" placeholder="Search tokens or agents..." /></div><div class="filters">${['All','Active','New','Trending'].map(f=>`<button class="filter ${filter===f?'active':''}" data-filter="${f}">${f}</button>`).join('')}</div></div><div class="token-grid">${visible.length?visible.map(tokenCard).join(''):`<div class="empty-state"><p class="eyebrow">No launches yet</p><h3>The registry is empty</h3><p>Create the first token to populate Discover.</p><a class="button button-primary" href="#/launch" data-route>Launch a token ${icon('arrow-up-right')}</a></div>`}</div></div>`);document.querySelector('#search').addEventListener('input',e=>{query=e.target.value;discover()});document.querySelectorAll('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{filter=btn.dataset.filter;discover()}))}
function tokenCard(t){return `<a class="token-card" href="#/token/${t.id}" data-route><div class="card-top"><div class="card-token"><div class="token-orb">${t.initials}</div><div><strong>${t.name}</strong><span>${t.ticker}</span></div></div><span class="agent-status">Active</span></div><p class="card-description">${t.description}</p><div class="card-bottom"><div><span>Fees</span><strong>${t.fees}</strong></div><div><span>Agent</span><strong>${t.balance}</strong></div><span class="card-link">View agent ${icon('arrow-up-right')}</span></div></a>`}
function detail(id){const t=tokens.find(item=>item.id===id)||tokens[0];shell(`<div class="container"><section class="detail-head"><a class="back-link" href="#/discover" data-route>${icon('arrow-left')} Back to Discover</a><div class="detail-title"><div class="token-orb">${t.initials}</div><div><h1>${t.name}</h1><p>${t.ticker}</p></div></div><p class="detail-description">${t.description}</p></section><section class="detail-content"><div class="stat-grid"><div class="stat"><span class="metric-label">Market cap</span><strong class="metric-value">${t.marketCap}</strong></div><div class="stat"><span class="metric-label">Volume · 24h</span><strong class="metric-value">${t.volume}</strong></div><div class="stat"><span class="metric-label">Holders</span><strong class="metric-value">${t.holders}</strong></div><div class="stat"><span class="metric-label">Fees generated</span><strong class="metric-value">${t.fees}</strong></div></div><div class="agent-layout"><section class="panel agent-panel"><p class="eyebrow">Autonomous agent</p><h2>AI Agent</h2><div class="status-line"><span class="status-dot"></span>Active · operating in preview</div><strong class="agent-balance">${t.balance}</strong><div class="balance-label">Available agent balance</div><div class="mission-panel"><span class="preview-label">Mission</span><p>“${t.mission}”</p></div></section><section class="panel agent-panel activity"><h3>Recent agent activity</h3>${t.activity.map((item,i)=>`<div class="activity-item">${icon(i===0?'cpu':'arrow-up-right')}<div><strong>${item}</strong><span>${i+1} day${i?'s':''} ago · executed automatically</span></div></div>`).join('')}</section></div></section></div>`)}
function docs(){shell(`<div class="container"><section class="page-head docs-hero"><p class="eyebrow">Inteagent protocol</p><h1>Tokens with a mission.</h1><p>Inteagent turns token activity into programmable funding for causes, communities and open projects.</p></section><div class="docs-layout"><aside class="docs-index"><span>On this page</span><a href="#docs-concept">The concept</a><a href="#docs-flow">How it works</a><a href="#docs-launch">Launching</a><a href="#docs-agent">The agent</a><a href="#docs-safety">Safety & transparency</a></aside><article class="docs-content"><section id="docs-concept"><p class="eyebrow">01 · The concept</p><h2>A token with a reason to exist.</h2><p>Inteagent is a launchpad where each token is created together with an AI agent and a clearly defined mission. The mission explains what the agent should do with fees generated by the token.</p><div class="docs-equation"><strong>TOKEN</strong><span>→</span><strong>FEES</strong><span>→</span><strong>AI AGENT</strong><span>→</span><strong>ACTION</strong></div></section><section id="docs-flow"><p class="eyebrow">02 · The flow</p><h2>From intent to execution.</h2><div class="docs-rows"><div><b>01</b><strong>Define the token</strong><p>Name, ticker, image and description establish the public identity.</p></div><div><b>02</b><strong>Write the mission</strong><p>The creator describes priorities, boundaries and the intended use of fees.</p></div><div><b>03</b><strong>Generate fees</strong><p>Trading activity creates the funding stream associated with the token.</p></div><div><b>04</b><strong>Execute actions</strong><p>The agent applies available funds to approved actions and records the result.</p></div></div></section><section id="docs-launch"><p class="eyebrow">03 · Launching</p><h2>Create a token and its mission together.</h2><p>The launch flow is intentionally more than a token form. A creator defines the asset, selects its visual identity and writes the operating intent of its agent. The live preview shows how that mission will be presented to users.</p><div class="docs-note"><span>${icon('info')}</span><p>A mission is not a vague slogan. It should be specific enough to guide decisions and transparent enough for supporters to evaluate.</p></div></section><section id="docs-agent"><p class="eyebrow">04 · The agent</p><h2>An execution layer for the mission.</h2><p>The AI agent is the operating identity attached to a token. It receives a defined mission, observes the available fee balance and proposes or performs actions that fit the mission.</p><div class="docs-columns"><div><strong>Mission</strong><p>What the creator wants to accomplish.</p></div><div><strong>Balance</strong><p>Funds available for permitted actions.</p></div><div><strong>Activity</strong><p>A public record of what happened.</p></div></div></section><section id="docs-safety"><p class="eyebrow">05 · Safety & transparency</p><h2>Autonomy needs boundaries.</h2><p>In the full product, agents must operate through explicit permissions: spending limits, approved recipients, action types, reserves and emergency pause controls. Every completed action should have a verifiable receipt.</p><ul class="docs-list"><li>Creators never provide private keys or seed phrases.</li><li>Agent permissions are narrower than wallet ownership.</li><li>Actions should be reviewable before and after execution.</li><li>Balances and activity should be visible to the community.</li><li>The current interface is a browser prototype, not a live financial protocol.</li></ul></section><div class="docs-end"><a class="button button-primary" href="#/launch" data-route>Launch with a mission ${icon('arrow-up-right')}</a><a class="button button-secondary" href="#/discover" data-route>Explore agents</a></div></article></div></div>`)}
function how(){home();setTimeout(()=>document.querySelector('.flow-section')?.scrollIntoView(),0)}
function route(){const path=location.hash.replace(/^#\/?/,'')||'';if(path==='launch')launch();else if(path==='discover')discover();else if(path==='docs')docs();else if(path==='how-it-works')how();else if(path.startsWith('token/')&&tokens.length)detail(path.split('/')[1]);else if(path.startsWith('token/'))discover();else home()}
let phantomWallet = null;
let walletConnectionInProgress = false;
let solanaModules = null;
async function loadSolana(){if(!solanaModules)solanaModules=Promise.all([import('https://esm.sh/@solana/web3.js@1.98.4?bundle'),import('https://esm.sh/@solana/spl-token@0.4.13?bundle')]);return solanaModules}
function getPhantom(){return window.phantom?.solana || (window.solana?.isPhantom===true?window.solana:null)}
async function connectWallet(){if(walletConnectionInProgress)return null;if(location.protocol==='file:'){showToast('Open the site through http://localhost:5500 for Phantom access.');return null}const provider=getPhantom();if(!provider){showToast('Phantom is not available in this browser. Open localhost in Chrome or Brave with the extension enabled.');return null}if(provider.isConnected&&provider.publicKey){phantomWallet=provider.publicKey.toString();updateWalletButton();await updateWalletBalance(provider.publicKey);return provider.publicKey}walletConnectionInProgress=true;try{const response=await provider.connect(),publicKey=provider.publicKey||response?.publicKey;if(!publicKey)throw new Error('Phantom did not return a public key. Check localhost:5500 in the extension.');phantomWallet=publicKey.toString();updateWalletButton();await updateWalletBalance(publicKey);showToast(`Connected ${phantomWallet.slice(0,4)}...${phantomWallet.slice(-4)}`);return publicKey}catch(error){const message=error?.code===4001?'Connection was rejected in Phantom.':error?.message==='Unexpected error'?'Phantom is busy with another tab. Close other Inteagent tabs, disconnect localhost:5500 in Phantom, then reload this page.':error?.message||'Phantom could not connect this site.';showToast(message);return null}finally{walletConnectionInProgress=false}}
function updateWalletButton(){const button=document.querySelector('.wallet-button span'),balance=document.querySelector('.wallet-balance');if(button)button.textContent=phantomWallet?`${phantomWallet.slice(0,4)}...${phantomWallet.slice(-4)}`:'Connect wallet';if(balance)balance.textContent=phantomWallet?'':'Phantom'}
async function updateWalletBalance(publicKey){const balance=document.querySelector('.wallet-balance');if(!balance||!publicKey)return;balance.textContent='...';try{const response=await fetch('https://api.devnet.solana.com',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({jsonrpc:'2.0',id:1,method:'getBalance',params:[publicKey.toString()]})}),data=await response.json();balance.textContent=data.result?`${(data.result.value/1e9).toFixed(3)} SOL`:'0 SOL'}catch(error){balance.textContent='Devnet'}}
function initWallet(){const provider=getPhantom();if(!provider)return;provider.on?.('connect',publicKey=>{phantomWallet=publicKey.toString();updateWalletButton();updateWalletBalance(publicKey)});provider.on?.('accountChanged',publicKey=>{if(!publicKey){phantomWallet=null;updateWalletButton();return}phantomWallet=publicKey.toString();updateWalletButton();updateWalletBalance(publicKey)});if(provider.isConnected&&provider.publicKey){phantomWallet=provider.publicKey.toString();updateWalletButton();updateWalletBalance(provider.publicKey)}}
window.addEventListener('phantom#initialized',initWallet);
function bindWalletButton(){const button=document.querySelector('.wallet-button');if(!button||button.dataset.bound)return;button.dataset.bound='true';button.addEventListener('click',()=>connectWallet())}
async function createDevnetToken(){const provider=getPhantom();if(!provider){await connectWallet();if(!phantomWallet)throw new Error('Connect Phantom before launching.')}return {mint:`browser-${crypto.randomUUID()}`,signature:'browser-local',network:'Browser Local'}}
function setupImageUpload(){const upload=document.querySelector('.upload');if(!upload||document.querySelector('#token-image'))return;upload.innerHTML=`${icon('image')}<label for="token-image">Choose token image</label><input id="token-image" type="file" accept="image/png,image/jpeg,image/webp" hidden><span id="image-name">· optional</span>`;document.querySelector('#token-image').addEventListener('change',event=>{const file=event.target.files[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{launchState.image=reader.result;document.querySelector('#image-name').textContent=file.name;const preview=document.createElement('img');preview.src=reader.result;preview.style.cssText='display:block;width:56px;height:56px;object-fit:cover;border-radius:50%;margin:12px auto 0;border:1px solid var(--line)';upload.appendChild(preview)};reader.readAsDataURL(file)})}
function setupNetworkNote(){const head=document.querySelector('.page-head');if(!head||document.querySelector('.network-note'))return;const note=document.createElement('p');note.className='network-note';note.textContent='Browser local registry · Phantom required to launch · data persists in this browser';head.appendChild(note)}
window.addEventListener('hashchange',()=>queueMicrotask(()=>{setupImageUpload();setupNetworkNote();bindWalletButton()}));queueMicrotask(()=>{setupImageUpload();setupNetworkNote();bindWalletButton()});
window.addEventListener('storage',event=>{if(event.key!=='inteagent-tokens')return;tokens.splice(0,tokens.length,...JSON.parse(event.newValue||'[]'));route()});
queueMicrotask(initWallet);
document.addEventListener('click',async event=>{const action=event.target.closest('[data-action]')?.dataset.action;if(action==='launch-submit'){event.stopImmediatePropagation();if(!phantomWallet){await connectWallet();if(!phantomWallet)return}const name=launchState.name.trim(),ticker=launchState.ticker.trim().toUpperCase(),mission=launchState.prompt.trim();if(!name||!ticker||!mission){showToast('Add a token name, ticker and agent mission.');return}const button=event.target.closest('[data-action="launch-submit"]');button.disabled=true;button.textContent='Waiting for Phantom...';try{const chain=await createDevnetToken(),id=name.toLowerCase().replace(/[^a-z0-9]+/g,'-')+'-'+Date.now(),token={id,name,ticker:ticker.startsWith('$')?ticker:`$${ticker}`,initials:name.split(/\s+/).map(word=>word[0]).join('').slice(0,2).toUpperCase(),description:launchState.description.trim()||'A token with an autonomous mission.',mission,image:launchState.image||'',fees:'$0',balance:'$0',actions:0,marketCap:'$0',volume:'$0',holders:'0',mint:chain.mint,signature:chain.signature,network:'Browser Local',activity:[]};tokens.push(token);localStorage.setItem('inteagent-tokens',JSON.stringify(tokens));document.querySelector('#modal [href^="#/token/"]').href=`#/token/${id}`;document.querySelector('#modal').classList.add('open');document.querySelector('#modal').setAttribute('aria-hidden','false');lucide.createIcons()}catch(error){showToast(error.message||'Token creation failed.');button.disabled=false;button.innerHTML=`Launch token ${icon('arrow-up-right')}`}}},true);
document.addEventListener('click',e=>{if(!e.target.closest('[data-action="launch-submit"]'))return;const name=launchState.name.trim(),ticker=launchState.ticker.trim().toUpperCase(),mission=launchState.prompt.trim(),modal=document.querySelector('#modal');if(!name||!ticker||!mission){modal.classList.remove('open');showToast('Add a token name, ticker and agent mission.');return}const id=`${name.toLowerCase().replace(/[^a-z0-9]+/g,'-')}-${Date.now()}`;const token={id,name,ticker:ticker.startsWith('$')?ticker:`$${ticker}`,initials:name.split(/\s+/).map(word=>word[0]).join('').slice(0,2).toUpperCase(),description:launchState.description.trim()||'A token with an autonomous mission.',mission,fees:'$0',balance:'$0',actions:0,marketCap:'$0',volume:'$0',holders:'0',activity:[]};tokens.push(token);localStorage.setItem('inteagent-tokens',JSON.stringify(tokens));document.querySelector('#modal [href^="#/token/"]').href=`#/token/${id}`;document.querySelector('#modal [href="#/discover"]').textContent='View registry'});
document.addEventListener('click',e=>{const action=e.target.closest('[data-action]')?.dataset.action;if(action==='menu'){document.querySelector('.desktop-nav').classList.toggle('mobile-open')}if(action==='close-modal'){document.querySelector('#modal').classList.remove('open');document.querySelector('#modal').setAttribute('aria-hidden','true')}});function showToast(message){const toast=document.querySelector('#toast');toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2600)}window.addEventListener('hashchange',route);route();lucide.createIcons();
